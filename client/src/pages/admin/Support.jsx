import { Header } from "../../components";

const Support = () => {
  return (
    <div>
      <Header
        heading="Support"
        subheading="This subheading exists because it is required to add a very breif detail about every page on the banner."
      />
    </div>
  );
};

export default Support;
