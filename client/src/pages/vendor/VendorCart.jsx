import { Header } from "../../components";

const VendorCart = () => {
  return (
    <div>
      <Header
        heading="Cart"
        subheading="This is a subheading for vendor's cart page. It is here because it is required to provide brief information about the page"
      />
    </div>
  );
};

export default VendorCart;
