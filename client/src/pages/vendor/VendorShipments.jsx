import { Header } from "../../components";

const VendorShipments = () => {
  return (
    <div>
      <Header
        heading="Shipments"
        subheading="This is a subheading for vendor's shipment page. It is here because it is required to provide brief information about the page"
      />
    </div>
  );
};

export default VendorShipments;
