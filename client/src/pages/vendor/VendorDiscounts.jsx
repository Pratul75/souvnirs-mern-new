import { Header } from "../../components";
const VendorDiscounts = () => {
  return (
    <div>
      <Header
        heading="Discounts"
        subheading="This is asubheading for vendor discounts, thijs heading is present because we need to provide the discounts info in header"
      />
    </div>
  );
};

export default VendorDiscounts;
